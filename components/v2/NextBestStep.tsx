// The dashboard's goal card is the Freedom Track card now (Phase 1 slice 5, components/FreedomTrack.tsx). This file
// keeps the old import path working for the screens that render it (CommandDashboard, ActionsScreen, PlayPage).
export { FreedomTrackCard as default } from '../FreedomTrack';
